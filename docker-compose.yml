services:
  app:
    build: .
    image: test
    container_name: app_test
    restart: always
    ports:
      - "8001:8080"
    networks:
      - app-db
networks:
  app-db:
    external: true  # mark it as an external network